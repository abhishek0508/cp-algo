// return true/false if there is pythogorean triplet in array
// expercted time complexity -> O(max(arr[i]^2))
// expercted space complexity -> O(max(arr[i]))
// approach is square the elements, sort the array, then find the triplet using two pointer approach
// or use hashing, use array elemenst as index

bool pythagorean_triplet(vector<int> nums){

}